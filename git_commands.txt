git init
git config --global user.name "Uttam"
git config --global user.email "devopswithuttam@gamil.com"
git status
git add filename
git commit -m "Message"
git restore filename
git push origin main

git checkout branchname

git push origin dev(Branch Name)